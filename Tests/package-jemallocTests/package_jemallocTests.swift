import XCTest
@testable import package_jemalloc

final class package_jemallocTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(package_jemalloc().text, "Hello, World!")
    }
}
